Key distribution Center
A set of two services, Authentication and Authorization, that provide the backbone of the Kerberos protocol. It works as a trusted middleman between Kerberos users.

# Why Authenticate and Authorize are Separate
Separating the KDC into two services allows administrators to separately manage what users are allowed on the network, and manage what resources those users are allowed to access